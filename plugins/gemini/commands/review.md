# /gemini:review

Use this command to perform a code review using the Gemini CLI.

## Instructions
When this command is invoked, execute the following shell command to request a code review from the Gemini CLI:

`gemini "Review the current uncommitted changes and provide feedback."`
